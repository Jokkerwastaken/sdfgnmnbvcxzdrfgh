// Point test
        System.out.printf("Point info: x: %.2f, y: %.2f", point.x, point.y);

        // Vector test
        System.out.printf("\nVector info: x: %.2f, y: %.2f", vector.x, vector.y);
        System.out.printf("\nVector normalization info: x: %.2f, y: %.2f", vector.normalize().x, vector.normalize().y);
        System.out.printf("\nVector dot product info: result: %.2f", vector.dotProduct(vector2));

        // Normal test
        System.out.printf("\nNormal info: x: %.2f, y: %.2f", normal.x, normal.y);

        GFrame frame = new GFrame();
        
        frame.canvas.addDrawable(point);
        frame.canvas.addDrawable(vector);
        frame.canvas.addDrawable(vector2);
        frame.canvas.addDrawable(normal);


        
        long lastTime = System.nanoTime();
        while (true) {
                long currentTime = System.nanoTime();
                float deltaTime = (currentTime - lastTime) / 1_000_000_000.0f;
                lastTime = currentTime;

                obj.move(deltaTime);
                frame.canvas.repaint();

                try {
                Thread.sleep(16);
                } catch (InterruptedException e) {
                e.printStackTrace();
                }
        }
